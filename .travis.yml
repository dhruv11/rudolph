sudo: required

language: go

services:
  - docker

go:
  - "1.10"

before_install:
  - go get github.com/adlio/trello
  - go get github.com/nlopes/slack

script:
  - go test -v
  - GOOS=linux GOARCH=amd64 go build main.go
  - docker build -t dhruvdhir/bot .