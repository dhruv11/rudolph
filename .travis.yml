sudo: required

language: go

services:
  - docker

go:
  - "1.10"

before_install:
  - go get github.com/adlio/trello
  - go get github.com/nlopes/slack

script:
  - go test -v
  - go build main.go
  - docker build -t dhruvdhir/bot .